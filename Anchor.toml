[features]
seeds = false
skip-lint = false
[programs.devnet]
chainlink_demo = "BGES1FPfijKk4st6LppwgZLVqxuMdnarcwezTDoKx172"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
chainlink_demo = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/chainlink_demo.ts"
